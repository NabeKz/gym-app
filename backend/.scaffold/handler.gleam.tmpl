import features/${FEATURE}/application
import gleam/json
import wisp.{type Request, type Response}

import app/handlers/error_responses
import app/handlers/validation

pub type ${FEATURE_PASCAL}Handler {
  ${FEATURE_PASCAL}Handler(
    // TODO: add handler fields
    // create: fn(Request) -> Response,
    // read: fn(String) -> Response,
    // list: fn(Request) -> Response,
  )
}

// TODO: implement handler functions
// fn create(create_fn: application.Create${FEATURE_PASCAL}, req: Request) -> Response {
//   todo
// }

pub fn new(
// TODO: add parameters
// create_fn: application.Create${FEATURE_PASCAL},
) -> ${FEATURE_PASCAL}Handler {
  todo
}
